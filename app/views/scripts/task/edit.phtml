<?php
$task = $this->task;

if (!is_array($task)) {
    $task = null;
}
?>

<main class="min-h-screen flex justify-center items-center p-10 px-5" style="background:#71D7E8; color:#18242B; font-family: Arial, sans-serif;">
    <section class="w-full max-w-[720px] rounded-[28px] p-10 box-border" style="background: rgba(255,255,255,0.88); box-shadow: 0 24px 60px rgba(0,70,90,0.22);">
        <h1 class="m-0 mb-3 text-5xl leading-none tracking-tight" style="color:#F76BA6;">Perfil de tarea</h1>

        <?php if (!empty($this->error)): ?>
            <p class="mb-5 py-3.5 px-4 rounded-2xl font-bold" style="background: rgba(233,54,69,0.12); color:#9D1022; border:1px solid rgba(233,54,69,0.35);">
                <?= htmlspecialchars($this->error, ENT_QUOTES, 'UTF-8') ?>
            </p>
        <?php endif; ?>

        <?php if (!empty($this->message)): ?>
            <p class="mb-5 py-3.5 px-4 rounded-2xl font-bold" style="background: rgba(12,182,132,0.12); color:#087E5B; border:1px solid rgba(12,182,132,0.35);">
                <?= htmlspecialchars($this->message, ENT_QUOTES, 'UTF-8') ?>
            </p>
        <?php endif; ?>

        <?php if (!empty($task)): ?>
            <form class="flex flex-col gap-[18px] mt-6" action="/task/edit" method="POST">
                <input
                        type="hidden"
                        name="id"
                        value="<?= htmlspecialchars($task['id'] ?? '', ENT_QUOTES, 'UTF-8') ?>"
                >

                <div class="flex flex-col gap-2">
                    <label for="title" class="text-[13px] font-extrabold uppercase tracking-wider" style="color:#F76BA6;">Título</label>
                    <input
                            type="text"
                            id="title"
                            name="title"
                            value="<?= htmlspecialchars($task['title'] ?? '', ENT_QUOTES, 'UTF-8') ?>"
                            required
                            class="w-full border-0 rounded-2xl py-4 px-[18px] text-base font-semibold outline-none box-border focus:ring-[3px]"
                            style="background: rgba(255,255,255,0.9); color:#18242B; --tw-ring-color: rgba(247,107,166,0.28);"
                    >
                </div>

                <div class="flex flex-col gap-2">
                    <label for="description" class="text-[13px] font-extrabold uppercase tracking-wider" style="color:#F76BA6;">Descripción</label>
                    <textarea
                            id="description"
                            name="description"
                            required
                            class="w-full border-0 rounded-2xl py-4 px-[18px] text-base font-semibold outline-none box-border resize-y min-h-[120px] focus:ring-[3px]"
                            style="background: rgba(255,255,255,0.9); color:#18242B; --tw-ring-color: rgba(247,107,166,0.28);"
                    ><?= htmlspecialchars($task['description'] ?? '', ENT_QUOTES, 'UTF-8') ?></textarea>
                </div>

                <div class="flex flex-col gap-2">
                    <label for="user_name" class="text-[13px] font-extrabold uppercase tracking-wider" style="color:#F76BA6;">Usuario</label>
                    <input
                            type="text"
                            id="user_name"
                            name="user_name"
                            value="<?= htmlspecialchars($task['user_name'] ?? '', ENT_QUOTES, 'UTF-8') ?>"
                            required
                            class="w-full border-0 rounded-2xl py-4 px-[18px] text-base font-semibold outline-none box-border focus:ring-[3px]"
                            style="background: rgba(255,255,255,0.9); color:#18242B; --tw-ring-color: rgba(247,107,166,0.28);"
                    >
                </div>

                <div class="flex flex-col gap-2">
                    <label for="status" class="text-[13px] font-extrabold uppercase tracking-wider" style="color:#F76BA6;">Estado</label>
                    <select
                            id="status"
                            name="status"
                            class="w-full border-0 rounded-2xl py-4 px-[18px] text-base font-semibold outline-none box-border focus:ring-[3px]"
                            style="background: rgba(255,255,255,0.9); color:#18242B; --tw-ring-color: rgba(247,107,166,0.28);"
                    >
                        <option value="PENDING" <?= ($task['status'] ?? '') === 'PENDING' ? 'selected' : '' ?>>
                            Pendiente
                        </option>

                        <option value="IN_PROGRESS" <?= ($task['status'] ?? '') === 'IN_PROGRESS' ? 'selected' : '' ?>>
                            En ejecución
                        </option>

                        <option value="FINISHED" <?= ($task['status'] ?? '') === 'FINISHED' ? 'selected' : '' ?>>
                            Finalizada
                        </option>
                    </select>
                </div>

                <div class="grid grid-cols-2 gap-[18px] max-[640px]:grid-cols-1">
                    <div class="flex flex-col gap-2">
                        <label for="created_at" class="text-[13px] font-extrabold uppercase tracking-wider" style="color:#F76BA6;">Inicio</label>
                        <input
                                type="text"
                                id="created_at"
                                value="<?= htmlspecialchars($task['created_at'] ?? '--:--', ENT_QUOTES, 'UTF-8') ?>"
                                readonly
                                class="w-full border-0 rounded-2xl py-4 px-[18px] text-base font-semibold box-border"
                                style="background: rgba(255,255,255,0.75); color: rgba(24,36,43,0.75);"
                        >
                    </div>

                    <div class="flex flex-col gap-2">
                        <label for="finished_at" class="text-[13px] font-extrabold uppercase tracking-wider" style="color:#F76BA6;">Fin</label>
                        <input
                                type="text"
                                id="finished_at"
                                value="<?= htmlspecialchars($task['finished_at'] ?? '--:--', ENT_QUOTES, 'UTF-8') ?>"
                                readonly
                                class="w-full border-0 rounded-2xl py-4 px-[18px] text-base font-semibold box-border"
                                style="background: rgba(255,255,255,0.75); color: rgba(24,36,43,0.75);"
                        >
                    </div>
                </div>

                <div class="flex justify-between items-center gap-[14px] flex-wrap mt-2.5 max-[640px]:flex-col max-[640px]:items-stretch">
                    <div class="flex gap-[14px] flex-wrap max-[640px]:flex-col max-[640px]:w-full">
                        <button
                                type="submit"
                                class="border-0 rounded-full py-4 px-6 text-base font-black cursor-pointer inline-flex items-center justify-center transition-transform transition-shadow duration-150 hover:-translate-y-0.5 active:translate-y-1 text-white max-[640px]:w-full"
                                style="background:#F76BA6; box-shadow: 0 8px 0 #D94D87;"
                        >
                            ✅ Guardar cambios
                        </button>

                        <button
                                type="submit"
                                formaction="/task/delete"
                                formmethod="POST"
                                class="border-0 rounded-full py-4 px-6 text-base font-black cursor-pointer inline-flex items-center justify-center transition-transform transition-shadow duration-150 hover:-translate-y-0.5 active:translate-y-1 text-white max-[640px]:w-full"
                                style="background:#F76BA6; box-shadow: 0 8px 0 #D94D87;"
                        >
                            🗑 Borrar tarea
                        </button>
                    </div>

                    <a
                            href="/test"
                            class="border-0 rounded-full py-4 px-6 text-base font-black cursor-pointer inline-flex items-center justify-center transition-transform transition-shadow duration-150 hover:-translate-y-0.5 no-underline max-[640px]:w-full"
                            style="background:#E5E7EB; color:#18242B; box-shadow: 0 8px 0 #B8BDC7;"
                    >
                        Cancelar
                    </a>
                </div>
            </form>

        <?php else: ?>
            <p class="mb-5 py-3.5 px-4 rounded-2xl font-bold" style="background: rgba(233,54,69,0.12); color:#9D1022; border:1px solid rgba(233,54,69,0.35);">
                No se pudo cargar la tarea seleccionada.
            </p>
        <?php endif; ?>
    </section>
</main>
