<style>
    body {
        margin: 0;
        min-height: 100vh;
        font-family: Arial, sans-serif;
        background: #71D7E8;
        color: #18242B;
    }

    .page {
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 40px 20px;
    }

    .card {
        width: 100%;
        max-width: 720px;
        background: rgba(255, 255, 255, 0.88);
        border-radius: 28px;
        padding: 40px;
        box-shadow: 0 24px 60px rgba(0, 70, 90, 0.22);
        box-sizing: border-box;
    }

    h1 {
        margin: 0 0 12px;
        font-size: 48px;
        line-height: 1;
        color: #F76BA6;
        letter-spacing: -1px;
    }

    .message {
        margin-bottom: 20px;
        padding: 14px 16px;
        border-radius: 16px;
        font-weight: bold;
    }

    .message.error {
        background: rgba(233, 54, 69, 0.12);
        color: #9D1022;
        border: 1px solid rgba(233, 54, 69, 0.35);
    }

    .message.success {
        background: rgba(12, 182, 132, 0.12);
        color: #087E5B;
        border: 1px solid rgba(12, 182, 132, 0.35);
    }

    .task-card {
        display: flex;
        flex-direction: column;
        gap: 18px;
        margin-top: 24px;
    }

    .form-group {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

    label {
        font-size: 13px;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: #F76BA6;
    }

    input,
    textarea,
    select {
        width: 100%;
        border: 0;
        border-radius: 18px;
        padding: 16px 18px;
        font-size: 16px;
        font-family: Arial, sans-serif;
        font-weight: 600;
        background: rgba(255, 255, 255, 0.9);
        color: #18242B;
        outline: none;
        box-sizing: border-box;
    }

    textarea {
        resize: vertical;
        min-height: 120px;
    }

    input[readonly],
    textarea[readonly],
    select:disabled {
        opacity: 1;
        cursor: default;
    }

    .meta-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 18px;
    }

    .actions {
        display: flex;
        gap: 14px;
        flex-wrap: wrap;
        margin-top: 10px;
    }

    .disabled-edit-btn,
    .cancel-btn,
    .delete-btn {
        border: 0;
        border-radius: 999px;
        padding: 16px 24px;
        font-size: 16px;
        font-weight: 900;
        cursor: pointer;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    .disabled-edit-btn {
        color: #FFFFFF;
        background: #F76BA6;
        box-shadow: 0 8px 0 #D94D87;
        opacity: 0.55;
        cursor: not-allowed;
    }

    .cancel-btn {
        color: #18242B;
        background: #E5E7EB;
        box-shadow: 0 8px 0 #B8BDC7;
    }

    .cancel-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 0 #B8BDC7;
    }

    .delete-form {
        margin: 0;
    }

    .delete-btn {
        color: #FFFFFF;
        background: #E93645;
        box-shadow: 0 8px 0 #A91522;
    }

    .delete-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 0 #A91522;
    }

    .delete-btn:active {
        transform: translateY(4px);
        box-shadow: 0 4px 0 #A91522;
    }

    @media (max-width: 640px) {
        .card {
            padding: 28px;
        }

        h1 {
            font-size: 38px;
        }

        .meta-grid {
            grid-template-columns: 1fr;
        }

        .actions {
            flex-direction: column;
        }

        .disabled-edit-btn,
        .cancel-btn,
        .delete-btn {
            width: 100%;
        }
    }
</style>

<?php
$task = $this->task;
?>

<main class="page">
    <section class="card">
        <h1>Perfil de tarea</h1>

        <?php if (!empty($this->error)): ?>
            <p class="message error"><?= htmlspecialchars($this->error) ?></p>
        <?php endif; ?>

        <?php if (!empty($this->message)): ?>
            <p class="message success"><?= htmlspecialchars($this->message) ?></p>
        <?php endif; ?>

        <?php if (!empty($task)): ?>
            <div class="task-card">
                <div class="form-group">
                    <label for="title">Título</label>
                    <input
                            type="text"
                            id="title"
                            value="<?= htmlspecialchars($task['title'] ?? '') ?>"
                            readonly
                    >
                </div>

                <div class="form-group">
                    <label for="description">Descripción</label>
                    <textarea id="description" readonly><?= htmlspecialchars($task['description'] ?? '') ?></textarea>
                </div>

                <div class="form-group">
                    <label for="user_name">Usuario</label>
                    <input
                            type="text"
                            id="user_name"
                            value="<?= htmlspecialchars($task['user_name'] ?? '') ?>"
                            readonly
                    >
                </div>

                <div class="form-group">
                    <label for="status">Estado</label>
                    <select id="status" disabled>
                        <option value="PENDING" <?= ($task['status'] ?? '') === 'PENDING' ? 'selected' : '' ?>>PENDING</option>
                        <option value="IN_PROGRESS" <?= ($task['status'] ?? '') === 'IN_PROGRESS' ? 'selected' : '' ?>>IN_PROGRESS</option>
                        <option value="FINISHED" <?= ($task['status'] ?? '') === 'FINISHED' ? 'selected' : '' ?>>FINISHED</option>
                    </select>
                </div>

                <div class="meta-grid">
                    <div class="form-group">
                        <label for="created_at">Inicio</label>
                        <input
                                type="text"
                                id="created_at"
                                value="<?= htmlspecialchars($task['created_at'] ?? '') ?>"
                                readonly
                        >
                    </div>

                    <div class="form-group">
                        <label for="finished_at">Fin</label>
                        <input
                                type="text"
                                id="finished_at"
                                value="<?= htmlspecialchars($task['finished_at'] ?? '--:--') ?>"
                                readonly
                        >
                    </div>
                </div>

                <div class="actions">
                    <button class="disabled-edit-btn" type="button" disabled>
                        Editar datos
                    </button>

                    <a class="cancel-btn" href="/task/create">
                        Cancelar
                    </a>

                    <form class="delete-form" action="/task/delete" method="POST">
                        <input
                                type="hidden"
                                name="id"
                                value="<?= htmlspecialchars($task['id'] ?? '') ?>"
                        >

                        <button class="delete-btn" type="submit">
                            🗑 Borrar tarea
                        </button>
                    </form>
                </div>
            </div>
        <?php elseif (empty($this->message) && empty($this->error)): ?>
            <p class="message error">
                No se pudo cargar la tarea seleccionada.
            </p>
        <?php endif; ?>
    </section>
</main>