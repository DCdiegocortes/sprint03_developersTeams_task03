<style>
    @font-face {
        font-family: 'OkineSans';
        src: url('/fonts/MADEOkineSansPERSONALUSE-Bold.otf') format('opentype');
        font-weight: 700;
        font-style: normal;
    }

    @keyframes cardEnter {
        from {
            opacity: 0;
            transform: translateY(18px) scale(0.98);
        }

        to {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
    }

    @keyframes bubbleTitle {
        0% {
            opacity: 0;
            transform: scale(0.72) translateY(18px);
        }

        65% {
            opacity: 1;
            transform: scale(1.08) translateY(0);
        }

        100% {
            opacity: 1;
            transform: scale(1) translateY(0);
        }
    }

    @keyframes fadeUp {
        from {
            opacity: 0;
            transform: translateY(14px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @media (max-width: 820px) {
        .home-card {
            flex-direction: column;
            align-items: flex-start;
        }

        .home-title {
            font-size: 38px;
        }

        .home-actions {
            width: 100%;
            justify-content: flex-start;
        }
    }

    @media (max-width: 560px) {
        .home-card {
            padding: 28px;
        }

        .home-title {
            font-size: 32px;
        }

        .home-actions {
            flex-direction: column;
            align-items: stretch;
        }

        .home-btn {
            width: 100%;
        }
    }
</style>

<main class="home-page">

    <?php if (isset($_GET['created']) && $_GET['created'] === '1'): ?>
        <p class="message success">
            Tarea creada correctamente!
        </p>
    <?php endif; ?>

    <section class="home-card">
        <div class="home-copy">
            <h1 class="home-title">¿Qué quieres hacer hoy?</h1>

            <p class="home-subtitle">
                Organiza tus tareas, revisa su estado y mantén el control del trabajo pendiente.
            </p>
        </div>

        <div class="home-actions">
            <a href="/task/create" class="home-btn home-btn-primary">
                + Nueva Tarea
            </a>

            <a href="/test" class="home-btn home-btn-secondary">
                🔍 Listar Tareas
            </a>
        </div>
    </section>
</main>