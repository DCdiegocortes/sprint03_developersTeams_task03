<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestor de Tareas</title>
    <style>
        * { box-sizing: border-box; }
        body { margin: 0; font-family: Arial, Helvetica, sans-serif; background: #f4f6f8; color: #1f2933; }
        .app-container { width: min(1180px, calc(100% - 32px)); margin: 0 auto; padding: 32px 0; }
        .dashboard-header { display: flex; justify-content: space-between; align-items: center; gap: 24px; margin-bottom: 24px; padding: 24px; background: #ffffff; border-radius: 18px; box-shadow: 0 8px 24px rgba(15, 23, 42, 0.08); }
        .dashboard-header h1 { margin: 0 0 8px; font-size: 32px; color: #111827; }
        .dashboard-header p { margin: 0; color: #6b7280; }
        .header-actions, .filter-actions, .form-actions, .quick-actions { display: flex; gap: 10px; flex-wrap: wrap; align-items: center; }
        .btn, .icon-btn { border: 0; border-radius: 999px; padding: 10px 16px; cursor: pointer; font-weight: 700; text-decoration: none; display: inline-flex; align-items: center; gap: 6px; }
        .btn-primary { background: #2563eb; color: #ffffff; }
        .btn-secondary { background: #111827; color: #ffffff; }
        .btn-light { background: #e5e7eb; color: #111827; }
        .btn-danger { background: #dc2626; color: #ffffff; }
        .icon-btn { background: #f3f4f6; color: #111827; font-size: 13px; padding: 8px 12px; }
        .panel { background: #ffffff; border-radius: 18px; padding: 22px; margin-bottom: 22px; box-shadow: 0 8px 24px rgba(15, 23, 42, 0.06); }
        .panel h2 { margin-top: 0; margin-bottom: 16px; color: #111827; }
        .input { width: 100%; border: 1px solid #d1d5db; border-radius: 12px; padding: 10px 12px; font-size: 15px; background: #ffffff; }
        textarea.input { min-height: 90px; resize: vertical; }
        .form-group { margin-bottom: 14px; }
        .form-group label { display: block; margin-bottom: 6px; font-weight: 700; color: #374151; }
        .filter-actions .input { max-width: 260px; }
        .tasks-board { display: flex; gap: 20px; align-items: flex-start; margin-bottom: 22px; }
        .task-column { flex: 1; background: #eef2f7; border-radius: 18px; padding: 16px; min-height: 260px; }
        .task-column h2 { margin-top: 0; font-size: 18px; }
        .task-card { background: #ffffff; border-radius: 16px; padding: 16px; margin-bottom: 14px; box-shadow: 0 6px 18px rgba(15, 23, 42, 0.08); border-left: 6px solid #d1d5db; }
        .task-card-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 12px; margin-bottom: 8px; }
        .task-card h3 { margin: 0; font-size: 17px; }
        .task-card p { color: #4b5563; margin: 8px 0; }
        .task-card small { display: block; color: #6b7280; margin-bottom: 12px; }
        .status-badge { border-radius: 999px; font-size: 12px; font-weight: 700; padding: 5px 10px; white-space: nowrap; }
        .task-pending { border-left-color: #facc15; background: #fffbeb; }
        .task-pending .status-badge { background: #fef3c7; color: #92400e; }
        .task-progress { border-left-color: #3b82f6; background: #eff6ff; }
        .task-progress .status-badge { background: #dbeafe; color: #1d4ed8; }
        .task-finished { border-left-color: #22c55e; background: #f0fdf4; }
        .task-finished .status-badge { background: #dcfce7; color: #166534; }
        .profile-card { border: 1px solid #e5e7eb; border-radius: 16px; padding: 18px; background: #f9fafb; }
        .profile-dates { display: flex; gap: 16px; flex-wrap: wrap; background: #ffffff; padding: 12px; border-radius: 12px; margin: 12px 0; }
        .profile-dates p { margin: 0; color: #374151; }
        .note { color: #6b7280; font-size: 14px; margin-top: -6px; margin-bottom: 14px; }
        @media (max-width: 900px) { .dashboard-header, .tasks-board { flex-direction: column; } .header-actions, .filter-actions { width: 100%; } .filter-actions .input { max-width: 100%; } .task-column { width: 100%; } }
    </style>
</head>
<body>
    <main class="app-container">
        <?php echo $this->content(); ?>
    </main>
</body>
</html>